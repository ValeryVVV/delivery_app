import { Box } from '@mui/material';
import { styled } from '@mui/system';

export const StyledCartList = styled(Box)({
    display: 'flex',
    flexDirection: 'row',
    paddingLeft: '30px',
    paddingRight: '30px',
    flexWrap: 'wrap',
    gap: '10px'
});
