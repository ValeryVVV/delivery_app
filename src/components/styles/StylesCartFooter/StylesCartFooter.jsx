import { Box } from "@mui/material";
import { styled } from '@mui/system';

export const StyledCartFooter = styled(Box)({
    display: 'flex', 
    justifyContent: 'space-between', 
    alignItems:'center', 
    padding: '10px', 
    height: '100px'
});
